/*.wasm
    Content-Type: application/wasm

/*.js
    Content-Type: application/javascript

/assets/*.ogg
    Content-Type: audio/ogg

/assets/*.ogg.meta
    Content-Type: application/json

# Force brotli encoding for .br files if accessed directly
# Cloudflare Pages should handle negotiation automatically for .wasm/.js vs .wasm.br/.js.br
# but we add this as requested.
/*.br
    Content-Encoding: brotli
